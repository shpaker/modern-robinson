# Дорожная карта до полной игры

Стек: **Go + Ebitengine**. Цель — проходимый ремейк, читающий ресурсы из папки
оригинальной игры.

## Этап 0. Реверс-инжиниринг форматов ✅

- [x] Распаковка ISO, инвентарь ресурсов (736 контейнеров, ~28k записей)
- [x] Контейнер `NL`: заголовок, директория, потоковый дешифр
- [x] Кодеки: `0x00` raw, `0x40` LZSS, `0x80` LZHUF, `0x100` DEFLATE — **100% декодируется**
- [x] Формат `NGB` (оба подтипа), палитра `COL` (Windows BGR)
- [x] Скриптовый язык `.SCN`/`.OB`/`.FS`; `.SCR` и сейв `.SAV`/`.BGI` — структурно
- [x] Устройство движка NGI (DLL, экспорты) — `docs/`

## Этап 1. Базовый движок (Go) ✅

- [x] Порт кодеков на Go + golden-сверка с Python
- [x] Парсеры сцен/объектов/скриптов, изосетка, pathfinding (BFS)
- [x] Рендер сцены (фон + палитра), персонаж (idle + ходьба `Rg_XX`, 8 сторон)
- [x] Ходьба по клику, переходы между локациями (`GoScene`), звук шагов
- [x] Режим отладки `F1`; Clean Architecture, тесты ядра, `just`, CI/release
- [x] Standalone-бинарник в папку игры; кросс-сборки Mac/Linux/Windows
- [x] Self-testing без окна (Ebiten `exp/vmhost`)

## Этап 2. Взаимодействие с миром ✅

- [x] Клик по объекту → подойти (`Aproach`) → проиграть действие `ROHAN<obj>`
- [x] Проигрыватель `.FS`: кадры (`Frame`/`Delay`, loop/one-shot) с событиями
- [x] Анимированные объекты сцены (огонь/дым/вода/краб) с Z-порядком
- [x] Канальный звук; `DelObject`/`GoScene` из действий
- [x] Курсоры по типу зоны (рука + стрелки); формула позиционирования объектов
- [x] Текст `Text` — реальные строки из `TEXT.DAT` (сделано на этапе 4)

## Этап 3. Логика квеста

- [x] Окно как в оригинале: viewport `640×480`, скролл сцены, место под панель
- [x] Флаги: `SetVar`/`AddVar`, `If`/`EndIf` (вложенность = И), `SetCharVar`
- [x] `CreateObject`/`DelObject`/`GoScene` из действий; правки мира персистентны
- [x] `SetVert` — переключение проходимости клеток
- [x] Панель инвентаря `BAR.BAR`/`BAR0.NGB`, иконки `<item>.mv`, выбор `SetActive`
- [x] Начальные переменные из `STARTUP.INF`; индекс всех сцен (`CAB_*`/`INT*`/`PALACE`)
- [x] Начальное состояние объектов из `BEGIN.BGI` (скрытые до `CreateObject`)
- [x] Пятница (`Frid`): спрайт/позиция (`Set`), `HideChar`/`ShowChar`, `Aproach`,
      7-арг `GoScene`, `FRHAN*`-действия, переключение портретом

## Этап 4. Тексты и озвучка

- [x] `TEXT.DAT` (CP1251, id = номер строки): имена объектов при наведении и
      реплики в `TextBox`; шрифт с кириллицей
- [x] Озвучка реплик — `Sound`-события кадров (уже в `.FS`)
- [x] Катсцены: текст поверх сцены при скрытой панели (`SetBar OFF`)

## Этап 5. Экраны и сохранения

- [x] Логотип и титул (`LOGO.DAT`), фейд, скип по клику; интро `INT0`→`INT1`
- [x] Save/Load: 12 слотов `robinson00..11.sav` (JSON) + миниатюры, `F5`/`F9`
- [x] Карта острова (`MAPSCR`) по кнопке после `SetMap ON`; entry-скрипты сцен
- [x] Кнопка карты идёт через `Roin0`/`Frin0` — герои за экраном, карта не ходибельна
- [x] Хит-тест зон как в движке: z сверху вниз, при равном z — первый по `ObjectList`
- [x] Музыка сцен и `SetMusic` (WAV-треки, зацикленный канал)
- [x] Главное меню (`OPTIONS.DAT`, оно же экран опций): 5 кнопок, ползунки звука/музыки/скорости
- [x] Меню открывается при запуске после титула, как в оригинале (ROBY.PDF, стр. 23): «Продолжить»/«Сохранить» погашены, пока партия не началась
- [x] 12 слотов сохранения с миниатюрами (экраны `SAVE`/`LOAD`)
- [x] Честные фейды из `.FAD` (16 палитровых шагов) на переходах

## Этап 6. Мини-игры

`MINIGAME.DLL` разобран — точные константы в [docs/10-minigames.md](docs/10-minigames.md).

- [x] `StartGame` передаёт экран мини-игре, результат — в квестовую переменную (0/1)
- [x] 5 — переводчик: сетка 26×10 (48,84) шаг 21×30, лента букв, случайная
      перестановка шифра, «стереть» = полный сброс
- [x] 4 — орган: перетаскивание 8 трубок в раструбы, мелодия из 15 нот,
      победа при верном порядке трубок
- [x] 1 — хижина: 17 деталей, целевые позиции, порядок сборки, поворот правой кнопкой
- [x] 0 — карта: 12 фрагментов, матрица смежности, склейка групп, допуск 10 px
- [x] 2 — шашки 6×6: ходы, обязательное взятие, дамки (ИИ — случайный ход с обязательным взятием, DLL не разобран)
- [x] 3 — воздушный шар: курс зависит от высоты, посадка при alt ≤ 175 и |Δ| < 50

## Этап 7. Полировка и релиз

- [x] Кнопки панели: карта острова и дискета — рисуются авторскими спрайтами
      (в фоне под ними вырезы); карта открывает остров, дискета — главное меню
- [x] Мерцание чёрного кадра при «Начать новую игру» (герой скрыт в INT0..INT4)
- [x] Экран «Загрузка» (`LOADING.NGB` из `LOGO.DAT`) закрывает переход от титула
      к вступительному ролику: мостик INT0 крутится за ним, распаковка `INT1.MV`
      идёт под ним — раньше сцена INT0 мелькала со спрайтом персонажа
- [x] Точная геометрия сцены по разбору `ROBY.EXE` (клетка/якорь/Shift, клик-зоны)
- [x] Ходьба = авторские циклы (движение в кадрах, клетка по `Shift char`), плавная камера
- [x] Голова в простое следит за курсором (`HEAD.MV` — таблица поз 3×3)
- [x] Выход из локации через скрипт: подход к краю, реплика, `GoScene`
- [x] Имя скрипта действия по персонажу и предмету в руке (+`SetCharVar`-варианты)
- [x] Скип катсцен (`Interrupt ON`), `SetRest`-простои и цепочки реплик
- [x] Ходьба по авторским циклам `DO.LST`; Пятница ходит (4 направления)
- [x] Обход всех 37 сцен headless — рендерятся без падений
- [x] Проверка логики квеста по данным (`just quest`): 4968 скриптов, 37 сцен —
      невыполнимых условий 0, недостижимых предметов 0, недостижимых сцен 0,
      сцен-ловушек 0 (INT4 — финал), все 6 мини-игр подключены (13 точек запуска)
- [x] Паковка релизов: `just release` (архивы с README и `config.yml`) + сборка
      артефактов в CI по тегу
- [x] Браузерная версия: `cmd/wasm`, оболочка в `web/`, ресурсы пакует
      `tools/packweb`, выкладка на хост данных — `just web-push` (rsync), CI
      собирает `cmd/wasm` — см. [docs/11-web-build.md](docs/11-web-build.md)

## Этап 8. Пропуски по мануалу (ROBY.PDF)

Сверка с руководством оригинала (`DOCUMENT/ROBY.PDF` на ISO, гл. 5–6, стр. 22–36)
и `docs/10-minigames.md`. Что мануал описывает и что сходится — меню, слоты,
ползунки, «рука», мозаики, криптограмма, обязательное взятие — проверено;
ниже только расхождения. (Посадка шара: в мануале «300 м», в DLL alt ≤ 175 —
код прав, мануал неточен.)

- [x] Кнопка-дискета панели открывает главное меню, а не экран сохранения
      (стр. 27; `internal/app/bar.go` `clickBar` → `openMenu`; сохранение
      остаётся пунктом меню)
- [ ] Реплика в текстбоксе живёт до наведения на активный предмет и вытесняется
      его именем, а не гаснет по таймеру (стр. 26; `effect.go`/`bar.go`;
      таймер оставить оверлею катсцен `SetBar OFF` и сервисным сообщениям)
- [ ] Шашки: дамка — только у шашки, **заканчивающей** ход на последней
      горизонтали; промежуточный прыжок цепочки взятий не промоует и цепочка
      продолжается по правилам простой (стр. 32;
      `internal/minigame/chess/chess.go` `apply` — промоутить, только если
      цепочка не продолжается)
- [ ] Шашки: за Робинзона первым ходит пират, за Пятницу — игрок (стр. 32;
      роль из `gs.ActiveChar` при `StartGame` — её придётся передать игре через
      `catalog`/`minigame.Host`, сама игра состояния квеста не видит;
      `aiWait=0.7` на старте и в `reset()` — рестарт после проигрыша тоже
      отдаёт ход пирату)
- [ ] Шашки: повторный клик по выбранной шашке снимает выделение (стр. 32)
- [ ] Карта-мозаика: раскидывать только `MapParts` найденных фрагментов, а не
      все 12 (стр. 33 + docs/10: первый запуск — 4, победа требует 12;
      при param ≤ 0 или > 12 — раскидывать 12)
- [ ] Орган: клик по Пятнице — «повторит свою арию» (стр. 31). Хотспот — bbox
      непрозрачных пикселей `SAMPLE.MV`: (71,203)–(158,368); звук
      `MELODY.WAV` лежит в `MINIGAME.WDT` (байт-в-байт = `SAMPLE.WAV`,
      22050/16/mono, 6.45 с) — играть через `host.PlaySound("melody.wav", 2)`;
      на время арии клики заблокированы. Опционально — анимация свиста декалями
      `SAMPLE.MV` (тайминги из `SAMPLE.FS`)
- [ ] F5/F9 не работают внутри мини-игр — «сохраняться во время встроенных игр
      нельзя» (стр. 28; `game.go`: к `mode == modePlay` добавить
      `&& g.mg == nil`; сейчас сейв из мини-игры теряет `mgResume`-хвост)
- [ ] Персонажи не проходят друг сквозь друга (стр. 36, «в тесных помещениях
      могут мешать один другому»). Механика оригинала вскрыта (`WalkTo
      0x40f63d..0x40f7c2`): на время построения пути движок временно вписывает
      в сетку `GridToClose` **всех** персонажей — тройки `dx,dy,d` из `.CHR`
      относительно их текущих клеток (`ROBY.CHR`: `0,0,2; 0,1,8`; `FRID.CHR`:
      `0,0,8; 0,0,9; 0,-1,2; 1,-1,1`), себя не исключая, — и восстанавливает
      сетку из бэкапа после. Т.е. блокируются не клетки, а входы-выходы
      (`Grid.SetDir` в ремейке уже есть); `GridToClose` из `.CHR` пока не
      парсится; скрытый персонаж мешать не должен
- [ ] Антураж мини-игр из `MINIGAME.SDT` (не из мануала, из данных): анимации
      и субтитры шашек (`RMOVE0/1`, `FMOVE0/1`, `P1MOVE0/1`, `P2MOVE0/1`,
      `VICTORY1/2`, `Text 1155–1158` — ход Пятницы/Робинзона,
      победа/поражение), органа (`LISTEN`, `TUB_IN/OUT`, `PIPE_IN/OUT`,
      `FEAR_IN/OUT`) и шара (`SEA`, `FALL`, `RAISE`, `FLY`) — в коде не
      используются вовсе
- [x] Выяснена роль `DATA/MOVIE/CANNOTDO.MV`: гипотеза «движковый фоллбэк»
      неверна — строки CANNOTDO в ROBY.EXE нет, это обычный ролик (Робинзон
      разводит руками), на который вручную ссылается 641 скрипт (самый частый
      ролик игры; у Пятницы аналог `FRNO.MV`). «Реакция на любые воздействия»
      (стр. 35) достигнута данными: скрипт на каждый предмет. Движок при
      отсутствии скрипта молчит — ремейк приведён к этому: выдуманный откат
      на `…HAN…` и показ имени объекта по клику убраны (имя живёт на hover),
      добавлен клик предметом «на себя» (5-буквенные `ROHAT`/`FRCON`…, 199
      скриптов; сравниваются клетки, с «рукой» клик съедается)
- [x] `docs/10-minigames.md`: `MELODY.WAV` — в `MINIGAME.WDT`, а не в
      `PIPE.DAT`; раздел «Статус в ремейке» обновлён (все 6 игр реализованы)

## Техдолг

- [x] Golden-тест раскладки всех 37 сцен (`internal/app/testdata/layout.golden`):
      сетка, клетки, origin спрайтов, клик-зоны, Z, блокировка; ловит сдвиг в 1 px
- [x] Конфиг игры (`config.yml`); трейс интерпретатора (`ROBINSON_TRACE`)
- [x] Тайминг шага: идёт по `Delay` кадров цикла, ползунок скорости масштабирует `dt`
- [x] Фоновый пул сцены (`SoundVariables` со `*`): птицы, чайки, сверчки —
      случайная запись со случайной громкостью и панорамой раз в 0..3 с
- [x] Скип катсцены глушит её звуки: состояние применяется, `Sound`/`Text` — нет;
      смена сцены обрезает эффект-каналы (крик из INT1 больше не тянется в игру)
- [x] Именованные `Sound name,ch`: канал игнорируется, звук играет своими
      голосами (третье поле `SoundVariables`, `0x422dd0`): занятый единственный
      голос пропускает повтор (прибой SCENA0, 6,8 с, больше не перезапускается
      каждые 2,3 с), несколько голосов — по очереди, самый старый
      перезапускается; фоновый пул идёт через те же голоса
- [x] Третий аргумент `Sound name,ch,N` — не обрезка: звук всегда играет целиком,
      а `RetimeByVoice` (`0x416F60`) при загрузке скрипта перезаписывает `Delay`
      `N` кадров (с кадра события) на `⌊длительность WAV⌋/N` — анимация
      подгоняется под озвучку (лип-синк наоборот; авторские Delay — заглушки
      0.17–6.2× от WAV). Реализовано в `use_cases.RetimeByVoice` + `Game.parseFS`
      на всех путях загрузки .FS (действия, входы, FonScript, idle, циклы)
- [ ] Ресемплинг WAV с частотой ≠ 22050 Гц: `yawn.wav` (44100) играет вдвое
      медленнее — `wav.DecodeF32` не приводит частоту к контексту
- [ ] Каналы вне 1..9 в данных (`Sound ship,54`/`end2,54` в INT4) — проверить, что
      слоты не размножаются бесконтрольно
- [x] `Aproach` — событие воспроизведения: фильм действия стоит на кадре с
      `Aproach`, пока персонаж не дойдёт (как в `ROBY.EXE`), так что команда
      работает в любом кадре — 656 скриптов с `Aproach` в кадре 1+
      (`SHIP2/ROHANOUT` — кадр 2, пары `FR*GOL`/`GOR` — кадр 1, финальные
      отходы длинных катсцен) больше не теряют её. Маршрут считается в момент
      события, поэтому порядок `SetVert`→`Aproach` выпал сам и `preAproachVerts`
      удалён; фаза «дойти, потом играть» (`pendingAct`) тоже — кадр 0 стартует
      кликом. Скип катсцены досаживает ходока в цель мгновенно и глушит
      презентацию — блокирующемуся плееру обход не нужен. Попутно: шаг
      `Shift Frid,X,±1` из циклов больше не сбрасывает `fridPath` через
      `fridSync` (Пятница дорисовывалась позой простоя после первого же шага)
- [ ] Фильм действия Пятницы z-сортируется по клетке Роби: `drawAction` зовётся
      из `drawCharacter`, чтобы фильм играл даже при `HideChar`, но слот в
      z-порядке при этом чужой
- [ ] F5 между кадром 0 и последним кадром действия сохраняет временный `SetVert`
      навсегда: `MarkVert` пишет открытие лужи в сейв, а закрытие кадра 29 в
      снимок уже не попадает
- [x] `ClosedDir` реализован по реверсу (`TryStep 0x410d00`): тройка `x,y,d` —
      асимметричный запрет выхода из клетки в нумпад-направлении `d`; в 23
      сценах (пальма SCENA0, дуб SCENA1, каюты). Плюс вскрыт и починен
      потерянный парсинг **объектных** `ClosedVert`/`ClosedDir` из `.OB`
      (крокодил — 11 клеток, краб, бочки; 50 объектов): ставятся при появлении,
      снимаются при удалении (`Object::UnClose`), иначе брод остаётся замурован;
      сценовые заборы — в golden (`fences`)
- [x] Смена цели на ходу не обрывает играющий цикл (`WalkTo 0x40f110`):
      новый путь — от клетки, куда цикл уже шагает, стык через поворот;
      клик по объекту на ходу не снапает героя в idle
- [x] `SetMouse OFF` подключён по реверсу: глушит клики и hover сцены (панель
      гейтит только `LockBar`, клик-скип катсцен живёт и при OFF), курсор —
      «часы»; движок сам выключает мышь на старте каждого муви и включает по
      концу (на это полагается 51 скрипт с одним лишь OFF) — повторено в
      `beginAction`/`updateAction`/`skipCutscene`; после загрузки сейва мышь
      форсится ON (`0x4213cf`) — повторено в `types.Restore`
